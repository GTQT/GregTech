package gtqt.api.capability;

/**
 * Marker for dynamic ME pattern provider buffers that must never be merged with
 * other input handlers during recipe matching.
 */
public interface IPatternBufferIsolatedHandler {
}
